# .env.development
COOKIE_SECRET = exampleSecret
JWT_SECRET = exampleSecret
DB_CONNECTION = mysql
PORT = 3000

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="mysql://root:@localhost:3306/db_dot_nestjs"

# .env.test
COOKIE_SECRET = exampleSecret
JWT_SECRET = exampleSecret
DB_CONNECTION = mysql
PORT = 3000
DATABASE_URL="mysql://root:@localhost:3306/db_learn_nestjs"

EMAIL = email@gmail.com # ganti dengan email yang valid
PASSWORD = password # ganti dengan password yang vaid
VALID_TOKEN = harusValidTokenDariResponseLogin  # harus diambil dari response login, perbarui juga jika sudah expired

# .env.production
COOKIE_SECRET = exampleSecret
JWT_SECRET = exampleSecret
DB_CONNECTION = mysql
PORT = 3000

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="mysql://root:@localhost:3306/db_dot_nestjs"


